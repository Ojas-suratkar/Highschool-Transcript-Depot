Frontend (apps/frontend) - NOTE: source remains at repo root `src/` for now

This project uses Vite + React + TypeScript.

Quick links (local layout - minimal reorg):
- Source: `./src/` (entry: `src/main.tsx` -> `src/App.tsx`)
- Pages: `./src/pages/`
- Components: `./src/components/`
- Build: `npm run build` (root `package.json`)

Why this folder exists
- A lightweight `apps/frontend/` placeholder was added to make the repo easier to navigate
  without moving the source files yet. In a later step we can `git mv` the source into
  this folder if you want a full physical move.
